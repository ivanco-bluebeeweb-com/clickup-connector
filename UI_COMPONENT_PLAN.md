# ClickUp Connector — UI Component Plan

## 1. Left Sidebar Architecture
- Слот: `slot="left"`, ширина: `align="stretch"`.
- Трёхрежимный блок входа (OAuth, Token, Client Credentials).
- Модальное окно `_help_modal()`.
- Кнопка `_settings_button()` в самом низу.
